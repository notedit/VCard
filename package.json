{
  "name": "vcard-m1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "sandbox/*"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "typecheck": "npm run -ws typecheck",
    "test": "npm run -ws test"
  }
}
