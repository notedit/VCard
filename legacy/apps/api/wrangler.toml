name = "vcard-api"
main = "src/worker.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# Local-dev secrets live in apps/api/.dev.vars (gitignored).
# Real values come from ~/.secrets/common.env per CLAUDE.md.
#
# Current API scope is synchronous deck CRUD + local generation snapshots.
# Image generation queues, R2 storage, Durable Objects, and agent sessions were
# removed with the old M1 plan and should be reintroduced only when the API
# contract needs them again.
