# Cloudflare Workers 静态资产部署 — 纯静态站,无 Worker 脚本
# 部署: npm run deploy  (= vite build && wrangler deploy)
# 首次需 `wrangler login` 或配置 CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID
name = "vcard-landing"
compatibility_date = "2025-01-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
